# Прод (Vercel) + API на отдельном хосте — без завершающего слэша:
# VITE_API_URL=https://re-store-backend.onrender.com

# Локально: Vite проксирует /api и /uploads (см. vite.config.ts)
VITE_API_URL=/api
